import 'package:flutter/material.dart';

class PenilaianKelulusanMahasiswa extends StatelessWidget {
const PenilaianKelulusanMahasiswa({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold();
  }
}